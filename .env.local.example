NEXT_PUBLIC_API_URL=http://localhost:3001
NEXT_PUBLIC_WS_URL=http://localhost:3001
# Public origin used to build sharable invite links (rooms). Required in prod
# so invite URLs render correctly during SSR — defaults to localhost in dev.
NEXT_PUBLIC_WEB_ORIGIN=http://localhost:3000
