# Team Icon Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Criar o componente `TeamIcon` e usá-lo em `LineupGrid` e `MatchCard` para exibir o escudo real do time quando disponível, com fallback para o quadrado de cor atual.

**Architecture:** Um componente `TeamIcon` isolado centraliza a lógica de imagem vs. cor. `LineupGrid` e `MatchCard` trocam seus divs de cor inline pelo novo componente. Nenhum outro componente é alterado.

**Tech Stack:** React, TypeScript, Tailwind CSS, Vitest + @testing-library/react

---

### Task 1: Criar o componente `TeamIcon`

**Files:**
- Create: `src/components/TeamIcon.tsx`
- Create: `src/components/TeamIcon.test.tsx`

- [ ] **Step 1: Escrever os testes que falham**

Crie `src/components/TeamIcon.test.tsx`:

```tsx
import { render, screen } from '@testing-library/react'
import { describe, expect, it } from 'vitest'
import { TeamIcon } from '@/components/TeamIcon'

describe('TeamIcon', () => {
  it('renders an img element when imageUrl is provided', () => {
    render(
      <TeamIcon
        imageUrl="https://example.com/team.png"
        primaryColor="#ff0000"
        secondaryColor="#ffffff"
      />,
    )
    expect(screen.getByRole('img')).toHaveAttribute('src', 'https://example.com/team.png')
  })

  it('renders no img element when imageUrl is null', () => {
    render(
      <TeamIcon
        imageUrl={null}
        primaryColor="#ff0000"
        secondaryColor="#ffffff"
      />,
    )
    expect(screen.queryByRole('img')).not.toBeInTheDocument()
  })
})
```

- [ ] **Step 2: Rodar os testes e confirmar que falham**

```bash
npx vitest run src/components/TeamIcon.test.tsx
```

Esperado: FAIL — `TeamIcon` não existe ainda.

- [ ] **Step 3: Implementar o componente**

Crie `src/components/TeamIcon.tsx`:

```tsx
import { cn } from '@/lib/utils'

const sizeMap = {
  sm: 'w-6 h-6',
  md: 'w-8 h-8',
} as const

interface TeamIconProps {
  imageUrl: string | null
  primaryColor: string
  secondaryColor: string
  size?: keyof typeof sizeMap
}

export function TeamIcon({ imageUrl, primaryColor, secondaryColor, size = 'md' }: TeamIconProps) {
  const sizeClass = sizeMap[size]

  if (imageUrl) {
    return (
      <div className={cn(sizeClass, 'rounded shrink-0 overflow-hidden flex items-center justify-center bg-black/30')}>
        <img src={imageUrl} alt="" className="w-full h-full object-contain p-0.5" />
      </div>
    )
  }

  return (
    <div
      className={cn(sizeClass, 'rounded shrink-0')}
      style={{ backgroundColor: primaryColor, border: `2px solid ${secondaryColor}` }}
    />
  )
}
```

- [ ] **Step 4: Rodar os testes e confirmar que passam**

```bash
npx vitest run src/components/TeamIcon.test.tsx
```

Esperado: 2 passed.

- [ ] **Step 5: Commit**

```bash
git add src/components/TeamIcon.tsx src/components/TeamIcon.test.tsx
git commit -m "feat(team-icon): add TeamIcon component with image/color fallback"
```

---

### Task 2: Usar `TeamIcon` no `LineupGrid`

**Files:**
- Modify: `src/components/LineupGrid.tsx`

- [ ] **Step 1: Rodar os testes existentes do LineupGrid (devem passar antes da mudança)**

```bash
npx vitest run src/components/LineupGrid.test.tsx
```

Esperado: 4 passed. Os testes usam `imageUrl: null`, então o fallback de cor garante compatibilidade.

- [ ] **Step 2: Substituir o div de cor pelo `TeamIcon` no header da coluna**

Em `src/components/LineupGrid.tsx`, adicione o import e troque o div no `TeamColumn`:

```tsx
// Adicionar import no topo do arquivo (junto dos outros imports de componentes):
import { TeamIcon } from '@/components/TeamIcon'

// Em TeamColumn, substituir:
// ANTES:
// <div
//   className="w-6 h-6 rounded shrink-0"
//   style={{ backgroundColor: team.primaryColor, border: `1px solid ${team.secondaryColor}33` }}
// />
//
// DEPOIS:
<TeamIcon
  size="sm"
  imageUrl={team.imageUrl}
  primaryColor={team.primaryColor}
  secondaryColor={team.secondaryColor}
/>
```

O bloco `<header>` completo dentro de `TeamColumn` deve ficar assim após a mudança:

```tsx
<header className="flex items-center gap-2 mb-3">
  <TeamIcon
    size="sm"
    imageUrl={team.imageUrl}
    primaryColor={team.primaryColor}
    secondaryColor={team.secondaryColor}
  />
  <span className="text-sm font-semibold">{team.abbreviation}</span>
  {missingCount > 0 && (
    <span className="text-xs text-muted-foreground ml-auto">{entries.length}/11</span>
  )}
</header>
```

- [ ] **Step 3: Rodar os testes do LineupGrid e confirmar que continuam passando**

```bash
npx vitest run src/components/LineupGrid.test.tsx
```

Esperado: 4 passed.

- [ ] **Step 4: Commit**

```bash
git add src/components/LineupGrid.tsx
git commit -m "feat(lineup-grid): replace color div with TeamIcon in column header"
```

---

### Task 3: Usar `TeamIcon` no `MatchCard`

**Files:**
- Modify: `src/components/MatchCard.tsx`

- [ ] **Step 1: Rodar os testes existentes do MatchCard (devem passar antes da mudança)**

```bash
npx vitest run src/components/MatchCard.test.tsx
```

Esperado: 5 passed. Os testes usam `imageUrl: null`, então o fallback de cor garante compatibilidade.

- [ ] **Step 2: Substituir o div de cor pelo `TeamIcon` no `TeamBadge`**

Em `src/components/MatchCard.tsx`, adicione o import e troque o div no `TeamBadge`:

```tsx
// Adicionar import no topo do arquivo (junto dos outros imports):
import { TeamIcon } from '@/components/TeamIcon'

// Em TeamBadge, substituir:
// ANTES:
// <div
//   className="w-8 h-8 rounded shrink-0"
//   style={{
//     backgroundColor: team.primaryColor,
//     border: `2px solid ${team.secondaryColor}`,
//   }}
// />
//
// DEPOIS:
<TeamIcon
  size="md"
  imageUrl={team.imageUrl}
  primaryColor={team.primaryColor}
  secondaryColor={team.secondaryColor}
/>
```

O componente `TeamBadge` completo deve ficar assim após a mudança:

```tsx
function TeamBadge({ team, align }: { team: TeamDto; align: 'left' | 'right' }) {
  return (
    <div
      className={cn('flex items-center gap-2 min-w-0', align === 'right' && 'flex-row-reverse')}
    >
      <TeamIcon
        size="md"
        imageUrl={team.imageUrl}
        primaryColor={team.primaryColor}
        secondaryColor={team.secondaryColor}
      />
      <span className="hidden sm:block text-sm font-semibold truncate">{team.shortName}</span>
      <span className="sm:hidden text-sm font-semibold tabular-nums">{team.abbreviation}</span>
    </div>
  )
}
```

- [ ] **Step 3: Rodar os testes do MatchCard e confirmar que continuam passando**

```bash
npx vitest run src/components/MatchCard.test.tsx
```

Esperado: 5 passed.

- [ ] **Step 4: Rodar todos os testes do projeto para confirmar zero regressões**

```bash
npx vitest run
```

Esperado: todos os testes passando, nenhuma falha nova.

- [ ] **Step 5: Commit**

```bash
git add src/components/MatchCard.tsx
git commit -m "feat(match-card): replace color div with TeamIcon in TeamBadge"
```

---

### Task 4: Push

- [ ] **Step 1: Push para o remote**

```bash
git push
```
